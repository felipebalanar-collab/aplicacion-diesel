# Ejemplo de Configuración para AWS S3 (Futuro)
# Este archivo documenta cómo configurar almacenamiento S3
# cuando escale el proyecto más allá de almacenamiento local

# ========================================
# AWS S3 Configuration (Para Futuro)
# ========================================

# S3 Bucket Setup
# Crear bucket en AWS S3 console:
# 1. Bucket name: banco-datos-inyectores (o similar unique)
# 2. Region: us-east-1 (o tu región)
# 3. Block public access: ENABLED
# 4. Enable versioning: YES (para auditoría)
# 5. Enable encryption: AES-256

# IAM User Configuration
# Crear usuario en AWS IAM con política:
# {
#   "Version": "2012-10-17",
#   "Statement": [
#     {
#       "Effect": "Allow",
#       "Action": [
#         "s3:PutObject",
#         "s3:GetObject",
#         "s3:DeleteObject",
#         "s3:ListBucket"
#       ],
#       "Resource": [
#         "arn:aws:s3:::banco-datos-inyectores",
#         "arn:aws:s3:::banco-datos-inyectores/*"
#       ]
#     }
#   ]
# }

# Environment Variables (Render.com Dashboard)
# AWS_S3_REGION=us-east-1
# AWS_S3_BUCKET=banco-datos-inyectores
# AWS_S3_ACCESS_KEY_ID=<IAM User Access Key>
# AWS_S3_SECRET_ACCESS_KEY=<IAM User Secret Key>

# Alternativas a AWS S3:
# - DigitalOcean Spaces ($5/mo, más simple)
# - Cloudinary (mejor para imágenes, $87/mo)
# - MinIO (self-hosted)
# - Backblaze B2 ($0.006/GB, más barato)

# Cost Estimation:
# - AWS S3: $0.023/GB/mes + transferencia
# - DigitalOcean: $5/mo por 250GB
# - Cloudinary: Desde $87/mo

# Para este proyecto (< 1000 imágenes = ~50 GB):
# Recomendación: DigitalOcean Spaces (~$5/mes)
